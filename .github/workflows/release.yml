# Release workflow — uses npm Trusted Publishers (OIDC, no NPM_TOKEN).
# Disabled until the first manual publish is done. Uncomment the `on:` trigger
# below after the package exists on npm and trusted publisher is configured at
# https://www.npmjs.com/package/<name>/access
#
# name: Release
#
# on:
#   push:
#     branches: [main]
#
# concurrency:
#   group: release-${{ github.ref }}
#   cancel-in-progress: false
#
# jobs:
#   release:
#     name: Release
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#       pull-requests: write
#       id-token: write
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0
#
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm
#           registry-url: https://registry.npmjs.org
#
#       - run: npm ci
#
#       - name: Build
#         run: npm run build
#
#       - name: Create release PR or publish
#         uses: changesets/action@v1
#         with:
#           publish: npm run release
#           version: npm run version
#           commit: 'chore: release packages'
#           title: 'chore: release packages'
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
