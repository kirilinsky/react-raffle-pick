# Changesets

Run `npx changeset` to add a changeset describing your change. On merge to `main`, the release workflow opens (or updates) a "Version Packages" PR. Merging that PR publishes to npm and tags the release.

See https://github.com/changesets/changesets for full docs.
